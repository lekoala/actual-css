; Release tags carry no v prefix: 0.9.2, never v0.9.2 — the published tags on
; GitHub are bare, so `npm version patch` must produce the same shape.
tag-version-prefix=""

; The version commit message is the bare version too, matching its tag.
message=%s
