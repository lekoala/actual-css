# Alert

Use `.alert` for contextual messages.

```html
<div class="alert warning soft">Check this value.</div>
<div class="alert danger soft" role="alert">Payment failed.</div>
```

Use `role="alert"` only for urgent dynamic messages.
