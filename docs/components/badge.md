# Badge

Use `.badge` for short status labels.

```html
<span class="badge success soft">Published</span>
<span class="badge warning outline">Pending</span>
```

Badges are non-interactive by default.
